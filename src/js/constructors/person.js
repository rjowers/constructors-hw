function Person (options) {
  options = options || {};
  this.pet = function(sadie){ sadie.status = "happy" }

Person.prototype.feed = function(moonshine)   {moonshine.hungry = false;
};

  this.cool = (options.cool == undefined)

}

export { Person };
